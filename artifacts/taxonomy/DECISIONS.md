# Taxonomy review decision log

Status: approved without taxonomy changes.

Approved taxonomy version: `v1`  
Approved digest: `d5270d9bd619f1316fcfb599145c51919f1d0e6110f1a6eb8cddb8c20eeecdab`

The approval was supplied explicitly in the analysis-engineer task handoff.
The human approval-notes field was blank, so no rationale has been invented.
The proposed merges below and the unclustered `Record keeping` category are
therefore retained exactly as reviewed. Any later split or merge requires a new
version, digest, and approval; it must not rewrite this record.

Do not add an approval record until the reviewer has inspected the exact digest
in `proposal.json`. Preserve each reviewed proposal and append decisions rather
than rewriting history.

## Questions for the first review

- Should `Conflict resolution` remain under **Complaint handling**, or become a
  broader separate skill?
- Should **Payment processing** combine cash handling and general payment work?
- Should **Reservation systems** combine generic booking systems with hotel PMS
  proficiency, or should software-operation specificity be retained?
- Should `Accuracy` remain merged with **Attention to detail**?
- Should `Prioritization` remain merged with **Time management**?
- **AI literacy** is flagged potentially emergent and model-specific in fixture
  data. It must be retained for a real run, but no fixture pattern is empirical.

## History

- Proposal v1: generated from labelled fixture/demo strings; unapproved.
- Approval v1: exact digest approved in the task handoff; categories and mappings
  unchanged. Approval record: `approval.json`.
