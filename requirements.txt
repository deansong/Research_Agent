# Core -- always needed.
langgraph
langgraph-checkpoint-sqlite
pydantic>=2
typing-extensions

# The one implemented model provider. Needed unless you only use --backend fake.
openai-codex

# For the stub backends, once you implement them (see agent/backends/):
#   anthropic     # agent/backends/api.py, provider="anthropic"
#   openai        # agent/backends/api.py, provider="openai"
# agent/backends/claude_code.py shells out to the `claude` CLI and
# agent/backends/antigravity.py to `agy` -- neither needs a package, only
# the command on PATH and logged in.

# The web UI (python main.py web .). Optional -- `run` works without them, and
# cli.py says so if you try `web` and they are missing.
fastapi
uvicorn[standard]

# webui/tests only. tree-sitter parses the browser modules so a stray bracket
# fails a test instead of producing a blank page; the check skips if absent.
#   httpx                 # FastAPI's TestClient needs it
#   tree-sitter
#   tree-sitter-javascript
#   quickjs               # actually EXECUTES the browser modules against a
#                         # stub DOM, which is the only way to catch a runtime
#                         # error at startup without a browser
